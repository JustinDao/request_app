= form_for @residence do |f|
  - if @residence.errors.any?
    #error_explanation
      %h2= "#{pluralize(@residence.errors.count, "error")} prohibited this residence from being saved:"
      %ul
        - @residence.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :name
    = f.text_field :name
  .actions
    = f.submit 'Save'
